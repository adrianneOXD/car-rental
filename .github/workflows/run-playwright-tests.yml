name: run playwright tests on feature branches

on:
  push:
    branches:
      'features/*'

jobs:
   run-tests:
     runs-on: ubuntu-latest


     steps:
       - name: Checkout
          uses: actions/checkout@v4.1.7


      - name: run test
        run: npx playwright test
